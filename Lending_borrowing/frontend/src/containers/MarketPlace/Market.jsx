import React from 'react';

export const Market = () => {
    return (
        <div>
          <button onClick={() => window.location.href = 'http://localhost:3000'}>Redirect to Port 3000</button>
        </div>
      );
  };
  